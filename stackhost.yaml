user:
  name: "Hexa Bot"
  github: "hexa-bot"

runtime:
  image: "python:3.11-slim"

commands:
  package: ""
  build:
    - "pip install -r requirements.txt"
  start: "python api.py"

repository:
  branch: "main"
  auto_deploy: true
